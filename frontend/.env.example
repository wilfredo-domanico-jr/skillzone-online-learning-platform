VITE_API_URL=http://localhost:8000

# Must match the backend's DEMO_MODE flag — shows one-click demo login
# buttons on the login page when true. Keep false except on a public
# demo/portfolio deployment.
VITE_DEMO_MODE=false
